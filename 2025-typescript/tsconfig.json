{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ES2020",                     // Output modern JS (ES2020)
    "module": "commonjs",                   // Use CommonJS modules (Node.js default)
    "lib": ["ES2020"],                      // Include modern JavaScript APIs

    /* Output Settings */
    "outDir": "./dist",                     // Compile output to the 'dist' folder
    "rootDir": "./src",                     // Your TypeScript source files

    /* Type Checking */
    "strict": true,                         // Enable all strict type-checking options
    "esModuleInterop": true,                // Allow default imports from CommonJS modules
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,                   // Skip type checking for .d.ts files for speed

    /* Node.js Support */
    "moduleResolution": "node",             // Use Node module resolution
    "types": ["node"]                       // Include Node.js type definitions
  },
  "include": ["src/**/*"],                  // Compile all files in 'src'
  "exclude": ["node_modules", "dist"]       // Ignore dependencies and build output
}
